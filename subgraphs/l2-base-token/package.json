{
  "name": "l2-base-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ l2-base-token",
    "create-local": "graph create --node http://localhost:8020/ l2-base-token",
    "remove-local": "graph remove --node http://localhost:8020/ l2-base-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 l2-base-token",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.86.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}